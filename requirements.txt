# requirements.txt — fiabilisation_cifen
# Généré via pigar.

# --- Scraping / navigation web ---
playwright==1.61.0
beautifulsoup4==4.15.0
requests==2.34.2

# --- Traitement PDF / OCR ---
docling==2.111.0
pypdf==6.14.2
PyMuPDF==1.28.0
pytesseract==0.3.13
Pillow==12.3.0

# --- Extraction de tableaux (MinerU) ---
# mineru installe automatiquement ses propres dépendances
# (opencv-python, python-docx, openpyxl, fastapi, etc.)
mineru==3.4.3

# --- Traitement de données ---
pandas==3.0.3
numpy==1.26.4

# --- Recherche web (diagnostics/complétion) ---
ddgs==9.14.4
duckduckgo_search==8.1.1

# --- Utilitaires ---
tqdm==4.68.4